<%= link_to '&#8592;'.html_safe, root_path %>
<h1>USA farmer's markets!</h1>

<div class = "markets_list">
  <ol>
    <% @markets.each do |market| %>
      <li><%= link_to "#{market.name}", "markets/#{market.id}" %></li>
      <% end %>
  </ol>
</div>
