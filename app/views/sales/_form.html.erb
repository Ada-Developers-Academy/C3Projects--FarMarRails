# THIS IS BROKEN!

<%= form_for [ @vendor, @sale ] do |f| %>
  <%= f.label :amount, "Sale amount (in cents):" %>
  <%= f.number_field :amount %>

  <%= f.label :purchase_time, "Sale date & time:" %>
  <%= f.datetime_field :purchase_time %>

  <%= collection_select

  <%= f.hidden_field :vendor_id, value: params[:vendor_id] %>

  <%= f.submit %>
<% end %>
