<h2>Add a Sale</h2>

<%= form_for @sale, url: @url do |f| %>

  <fieldset class="form">

  <div class="form_field">
    <%= f.label :amount, 'Sale Amount in Cents' %>
    <%= f.text_field :amount, required: "required" %>
  </div>

  <div class="form_field">
    <%= f.label :purchase_time, 'Purchase Date' %>
    <%= f.date_field :purchase_time %>
  </div>

  <div class="form_field">
    <%= f.label :product_id, 'Product' %>
    <%= f.collection_select :product_id, @vendor.products, :id, :name, :prompt => true, :include_blank => true %>
  </div>

    <%= f.hidden_field :vendor_id, :value => "#{@vendor.id}" %>

  </fieldset>
    <%= f.submit %>
<% end %>
