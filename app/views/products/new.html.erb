<h1>Create Product</h1>

<%= form_for @product, url: { action: "create"} do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :vendor_id %>
  <%= f.collection_select :vendor_id, Vendor.order(:name), :id, :name %>

  <%= f.submit %>

<% end %>
