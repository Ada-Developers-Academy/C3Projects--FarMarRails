<%= form_for @product, url: @url do |f| %>
  <fieldset class="form">

    <%= f.label :name, 'Product Name' %>
    <%= f.text_field :name %>

    <%= f.hidden_field :vendor_id, :value => "#{@vendor.id}" %>

  </fieldset>
    <%= f.submit %>
<% end %>
