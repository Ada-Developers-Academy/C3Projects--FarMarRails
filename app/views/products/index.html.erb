<h2><%= link_to "View products just for the current month.", "./current", title: "Click here to review this month's products." %></h2>
<%= link_to "+ New Product", "/vendors/#{@vendor.id}/products/new" %>

<ul>
  <% @products.each do |product| %>
      <li>
        <div>
          <%= product.name %>
        </div>

        <div>
          Edit
        </div>

        <div>
          Delete
        </div>
      </li>
  <% end %>
</ul>
