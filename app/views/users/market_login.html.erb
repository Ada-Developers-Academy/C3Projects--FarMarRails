<header>
  <h1>Welcome to the Market Login</h1>
</header>

<%= form_tag(controller: "markets", action: "dashboard") do %>
  <%= label_tag(:query, "Search for:") %>
  <%= text_field_tag(:query) %>
  <%= submit_tag("Search") %>
<% end %>
