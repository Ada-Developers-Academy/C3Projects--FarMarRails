<%= form_for @vendor, url: @url, method: @method do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :num_of_employees %>
  <%= f.text_field :num_of_employees %>

  <%= f.hidden_field :market_id, :value => params[:market_id] %>

  <%= f.submit %>

  <% end %>
