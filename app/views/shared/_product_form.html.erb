<%= form_for @product, url: @url, method: @method do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.hidden_field :vendor_id, :value => params[:vendor_id] %>

  <%= f.submit %>

  <% end %>
