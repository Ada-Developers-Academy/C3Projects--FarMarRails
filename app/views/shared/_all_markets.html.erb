<main>
  <table>
    <tr>
      <th>Market Name</th>
    </tr>
    <tr>
      <td>(click each for details)</td>
    </tr>
    <% @markets.each do |market| %>
    <tr>
      <td><%= link_to "#{market.name.titlecase}", "#{@market_path.market.id}" %> </td>
    </tr>
    <% end %>
  </table>
</main>
