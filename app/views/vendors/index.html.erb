<% content_for :breadcrumbs do %>
  <%= render partial: "/markets/dashboard" %>
<% end %>

<ul>
  <% @vendors.each do |vendor| %>
  <li>
    <%= vendor.name %>
    <%= button_to "/delete/#{ vendor.id }", data: { confirm: "Are you sure? This is permanent." } do %>
      Delete
    <% end %>
  </li>
  <% end %>
</ul>
