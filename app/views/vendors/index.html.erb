<%= render "shared/header" %>
  <fieldset>

  <%= form_for @vendor do |f| %>
  <%= f.label :id, "Vendor Id:"  %>
  <%= f.collection_select(:id, Vendor.all, :id, :id)  %>
  <%= f.submit %>
    <% end %>
  </fieldset>
