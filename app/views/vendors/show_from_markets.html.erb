<h1>Here's some basic info on this vendor</h1>

<%= render 'vendor_attributes' %>

<%= link_to 'Edit this vendor', edit_market_vendor_path(@market, @vendor) %>

<%= button_to 'Delete this vendor', market_vendor_path(@market, @vendor), method: :delete,data: { confirm: "Are you sure you want to delete this vendor?" } %>